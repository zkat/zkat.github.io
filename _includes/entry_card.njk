<article class="entry-card">
  <figure>
    <img alt="{{ entry.image.alt | default('') }}" src="{{ entry.image.src }}">
  </figure>
  <header>
    {{ entry.title }}
  </header>
</article>
{% if entry.image.attribution and showCredit %}
  <figcaption><a target="_blank" href="{{ entry.image.attribution }}">{{ entry.image.attribution }}</a></figcaption>
{% endif %}
